{
  "name": "oh-my-gemini-vscode",
  "displayName": "oh-my-gemini (OMG)",
  "description": "Visualize oh-my-gemini agents and workflows in VS Code",
  "version": "0.1.0",
  "publisher": "r3dlex",
  "license": "MIT",
  "engines": { "vscode": "^1.85.0" },
  "categories": ["AI", "Other"],
  "keywords": ["gemini", "multi-agent", "orchestration", "workflow"],
  "activationEvents": ["onStartupFinished"],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      { "command": "omg.showStatus", "title": "OMG: Show Status" },
      { "command": "omg.clearState", "title": "OMG: Clear State" }
    ],
    "viewsContainers": {
      "activitybar": [{ "id": "omg-explorer", "title": "oh-my-gemini", "icon": "resources/icons/omg-activity.svg" }]
    },
    "views": {
      "omg-explorer": [
        { "id": "omg.workflows", "name": "Active Workflows" },
        { "id": "omg.agents", "name": "Agents" },
        { "id": "omg.tasks", "name": "Tasks" }
      ]
    },
    "configuration": {
      "title": "OMG",
      "properties": {
        "omg.showStatusBar": { "type": "boolean", "default": true, "description": "Show status bar" }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --platform=node --format=cjs --minify",
    "watch": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --platform=node --format=cjs --watch",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.7.0"
  }
}
