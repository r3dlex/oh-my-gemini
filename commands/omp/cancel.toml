description = "Safely stop active work, preserve resumable context, and summarize what was interrupted."
prompt = """
Run the oh-my-product cancel skill to gracefully stop active work.

If args are provided:
  oh-my-product skill cancel {{args}}
  # alias: omp skill cancel {{args}}
Else:
  oh-my-product skill cancel
  # alias: omp skill cancel

Prefers graceful shutdown. Summarizes completed work, in-progress items, and persisted state locations.
Report the skill output to the user.
"""
