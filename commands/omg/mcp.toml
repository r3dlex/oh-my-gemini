description = "Run oh-my-gemini MCP stdio server with tools/resources/prompts surfaces."
prompt = """
Run the oh-my-gemini MCP server.

If args are provided:
  oh-my-gemini mcp serve {{args}}
  # alias: omg mcp serve {{args}}
Else:
  oh-my-gemini mcp serve --dry-run --json

Report:
1) resolved tool/resource/prompt surfaces,
2) whether stdio transport was started,
3) next command(s) for MCP client integration.
"""
